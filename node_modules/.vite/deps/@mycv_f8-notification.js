import "./chunk-G3PMV62Z.js";

// node_modules/@mycv/f8-notification/dist/index.es.js
var settings = {
  ignoreFocused: true,
  timeout: 0,
  cooldown: 0
};
var available = false;
var cooldownActive = false;
var focused = true;
function initNotifications(options) {
  if (!window.hasOwnProperty("Notification")) {
    console.info("This browser does not support notifications");
    return {
      then: function then(fn) {
        return fn(false);
      }
    };
  }
  settings = Object.assign({}, settings, options);
  window.onfocus = function() {
    focused = true;
  };
  window.onblur = function() {
    focused = false;
  };
  var availablePromise = new Promise(function(resolve, reject) {
    if (Notification.permission === "granted") {
      resolve(true);
    } else if (Notification.permission !== "denied") {
      Notification.requestPermission().then(function(permission) {
        if (permission === "granted") {
          resolve(true);
        }
        resolve(false);
      });
    } else {
      resolve(false);
    }
  });
  availablePromise.then(function(result) {
    available = result;
  });
  return availablePromise;
}
function notify(title, notifyOptions) {
  if (!available || settings.ignoreFocused && focused || cooldownActive) {
    return null;
  }
  var notification = new Notification(title, notifyOptions);
  notification.onclick = function() {
    window.focus();
    notification.close();
  };
  if (settings.timeout !== 0) {
    window.setTimeout(function() {
      notification.close();
    }, settings.timeout);
  }
  if (settings.cooldown !== 0) {
    cooldownActive = true;
    window.setTimeout(function() {
      cooldownActive = false;
    }, settings.cooldown);
  }
  return notification;
}
export {
  initNotifications,
  notify
};
//# sourceMappingURL=@mycv_f8-notification.js.map
